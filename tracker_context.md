# fNIRS FW 트래커 개선 컨텍스트 (외부 개발 인수인계용)

> 이 문서는 `fw_tracker.html`(https://wadehan-wis.github.io/fNIRS-fw-plan-tracker/fw_tracker.html)의
> **소스 코드를 직접 수정하지 않고**, 외부 개발자가 개선 작업을 시작할 수 있도록
> 현재 상태와 문제점, 실제 프로젝트 진행 상황과의 차이를 정리한 브리핑이다.
> (`~/.claude` 메모리 규칙: 트래커는 컨텍스트로만 작성, 직접 수정 금지)

작성일: 2026-09-18 (fNIRS FW v0.1.11 기준)

---

## 1. 트래커가 지금 하는 일 (as-is 요약)

단일 HTML 파일(로컬스토리지 기반 SPA)로, Rev0~3 펌웨어 개발을 **영업일 13일**
(2026-09-14~10-02) 단위로 추적한다.

- **데이터**: `TASKS`(21개), `ARCH_MAPPING`, `AT_ITEMS`(12개), `RISKS`(A1~A12),
  `DAYPLAN`(Day1~13), `DAY_DETAILS`, `WEEKLY_MILESTONES` — 전부 **JS 코드에 하드코딩**
  된 상수 배열/객체.
- **저장**: `localStorage['claude_artifact:fw_tracker_state']`에 진행률/체크/일일
  실적을 JSON으로 저장. 서버·DB 없음 — **브라우저(기기)를 바꾸면 데이터가 사라짐**,
  여러 사람이 공유해서 보는 용도로는 원천적으로 부적합.
- **탭**: 개요/아키텍처 결정/논문 재분석/Task 체크리스트/간트차트/Day Plan/AT/미확정 항목.

## 2. 지금 당장 눈에 보이는 문제 (개선 포인트)

1. **하드코딩된 계획과 실제 진행이 이미 크게 벌어짐.**
   `TASKS`/`DAYPLAN`은 "Rev0(9/14)→Rev1(Acquisition)→Rev2(BLE+OTA)→Rev3(Reliability)"
   순서로 짜여 있는데, 실제로는 Day 3~5(9/16~18) 사이에 **Rev2/Rev3 항목(OTA 실
   업데이트/롤백, Watchdog fault injection, BLE 로컬 버퍼링, safety state machine)을
   이미 다 구현·검증**했다. 코드 상 진행률(%)을 사람이 손으로 계속 끌어올려야 하고,
   이 구조 자체가 "계획이 실제를 못 따라가는" 근본 원인이다.
2. **단일 진실 소스(SoT) 부재 — 문서 3종이 서로 손으로 동기화됨.**
   `architecture.md`(§9 Rev단계/§11 미확정 항목) + 일일 업무일지(docx) + Gantt
   차트(xlsx) + 이 트래커, 4곳에 사실상 같은 정보(진행률, 미확정 항목, 오늘 한 일)가
   중복 기록된다. 지금은 매번 사람이 4곳을 다 손으로 맞추고 있음 — 자동화 여지가 큼.
3. **"필요 없는 개발 내용"**: 사용자가 지적한 부분 — 아래 §3에서 구체적으로 정리.
4. **공유 불가**: localStorage 특성상 팀/관리자와 상태를 공유하려면 스크린샷/캡처
   외엔 방법이 없다. 관리자가 물어볼 만한 질문(주간 보고서 관점, `~/.claude/CLAUDE.md`
   규칙 참고)에 답하려면 결국 별도 문서(일일 업무일지/주간 보고)를 또 써야 한다.
5. **AT_ITEMS/RISKS(A1~A12)도 architecture.md §10/§11과 별도로 관리**되고 있어, 한쪽만
   갱신하면 바로 어긋난다.

## 3. "필요 없는 개발 내용" — 정리 대상 후보

- **레거시 테스트 APK 프로토콜 상수**(Service UUID `0x1523`, CONFIG(0x1525) 10바이트
  레이아웃 등)가 트래커 코드에 그대로 박혀 있음 — 이건 `m_ble_proto.h`/
  `m_ble_gatt.h`에 이미 정확하게 문서화돼 있는 내용의 중복이다. 트래커에 다시
  하드코딩할 이유가 없음, 링크만 걸거나 제거 검토.
- **ARCH_MAPPING(S1↔S2 구조 비교표)**: Rev0 완료 시점(9/14)의 1회성 의사결정
  자료다. 이미 `architecture.md` §3에 정식으로 기록돼 있으므로 트래커에 별도
  탭으로 유지할 필요가 낮음.
- **"논문 재분석" 탭**: 논문 스펙 비교는 `architecture.md` §11 항목5에 이미
  정리돼 있음 — 트래커 안에 중복.
- **DAY_DETAILS(팁/검증/체크리스트)**: 실제로 오늘까지 하루도 이 형식대로
  안 쓰였고(일일 업무일지 docx로 대체 운영 중), 유지보수 부담만 있는 죽은 기능일
  가능성이 높음 — 실제 사용 여부 확인 후 제거/단순화 검토.

## 4. 실제 프로젝트 현재 상태 (2026-09-18, 외부 개발자가 알아야 할 최신 사실)

- 펌웨어 버전: **v0.1.11**, 변경 이력은 프로젝트 저장소 `CHANGELOG.md` 참고
  (v0.0.1~v0.1.11, 이 문서 작성 시점까지 전부 최신).
- 이미 실기 검증 완료: LED PWM duty, AS7341 raw data 획득(정성적 검증만),
  BLE 실 스택+테스트 APK 연동, Watchdog fault injection, BLE 로컬 버퍼링,
  OTA 업그레이드/다운그레이드/전원차단 롤백/손상이미지 거부/test-미확정 자동롤백.
- 아직 미착수/보류: BLE 페어링·암호화(테스트 앱 본딩 지원 확인 대기),
  AS7341 gain/ATIME 정밀 캘리브레이션(모바일 연동 데이터 수집 후),
  배터리/온도 관련 항목(PoC v1 보드 HW 제약으로 다음 리비전까지 보류).
- 미확정 항목 최신 목록: `architecture.md` §11 (항목1~7, 매번 갱신됨) — 트래커의
  `RISKS`(A1~A12)는 이 §11과 지금 **1:1로 안 맞을 가능성이 높음**, 재작업 시 §11을
  기준(SoT)으로 다시 매핑해야 한다.

## 5. 개선 방향 제안 (우선순위 순)

1. **하드코딩 데이터를 외부 파일(JSON)로 분리** — 최소한 코드 수정 없이 데이터만
   갱신 가능하게. 이상적으로는 `architecture.md`/`CHANGELOG.md`를 파싱해서 자동
   생성하는 스크립트를 만들면 SoT 중복 문제가 크게 줄어든다.
2. **공유 가능한 저장소로 전환** (localStorage → 간단한 백엔드 또는 Google
   Sheet/Notion 연동 등) — 관리자가 브라우저 없이도 상태를 볼 수 있게.
3. **§3에서 지목한 "필요 없는 내용" 제거/링크화** — 레거시 프로토콜 상수, ARCH_MAPPING,
   논문 재분석 탭은 정보량 대비 유지비용이 큼.
4. **Rev 단계 하드코딩을 없애고 "진행 로그" 방식으로 전환** — 계획된 순서가 아니라
   실제로 무엇을 완료했는지 타임라인으로 쌓는 방식이 지금 프로젝트의 실제 진행
   패턴(계획보다 앞서 여러 Rev를 오가며 진행)에 더 맞는다.
5. RISKS(A1~A12)를 `architecture.md` §11 항목과 ID 기준으로 재매핑.

---

이 문서는 트래커 재작업을 맡을 사람에게 그대로 전달하면 된다. 트래커 HTML/JS
자체는 이 세션에서 전혀 수정하지 않았다.
