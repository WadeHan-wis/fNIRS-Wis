# Rev0 | OTA(MCUboot+SMP)는 v1.0 필수 사양이나 구현은 Rev2 (agents.md §3-7, architecture.md §3/§9).
# 여기서는 sysbuild가 MCUboot를 child image로 빌드하도록 구조만 미리 켜 둔다.
# TODO(open-item): 실제 서명키/파티션 레이아웃(pm_static.yml)은 보드/플래시 크기 확정 후 추가 (Rev2).
SB_CONFIG_BOOTLOADER_MCUBOOT=y
